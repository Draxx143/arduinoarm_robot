# چرا بازو با کمترین سرعت می‌چرخید و چه چیزی درست شد

> خلاصه: سه باگ مستقل باعث می‌شد سرعت عملاً روی حداقل قفل شود و هیچ‌کدام از
> تنظیمات `Config.h` یا دستور `profile` اثری نداشته باشند. هر سه اصلاح شدند.

---

## ۱) تشخیص — سه علت اصلی

### علت ۱ — تایمر ۱ کیلوهرتز: سقف سرعت روی `1000 steps/s` قفل بود

```cpp
#define CONTROL_LOOP_FREQ 1000     // هر 1ms یک بار
ISR(TIMER1_COMPA_vect) { update(); }   // و در هر بار فقط «یک» استپ
```

`Axis::update()` در هر تیک **حداکثر یک استپ** صادر می‌کرد. یعنی حتی اگر
`AXIS_X_MAX_SPEED` را روی `20000` هم می‌گذاشتی، موتور هرگز سریع‌تر از
`1000 steps/s` (= **۲۲ درجه بر ثانیه** روی محور X) نمی‌چرخید.

**دقیقاً به همین دلیل بالا بردن `MAX_SPEED` و `ACCELERATION` در `Config.h`
هیچ اثری نداشت.**

### علت ۲ — سرعت هومینگ hard-code روی `100 steps/s`

```cpp
bool Axis::startHoming() {
    _currentSpeed = 100;                       // ← عدد ثابت، از Config.h نمی‌آید
    _stepInterval = 1000000UL / _currentSpeed;
}
```

هومینگ هیچ‌وقت از `_maxSpeed` یا `_acceleration` استفاده نمی‌کرد. نتیجه:
محور X برای رسیدن به endstop **۱۵۵ ثانیه** (۲.۵ دقیقه) راه می‌رفت و
مجموع هوم ۵ محور حدود **۶ دقیقه** طول می‌کشید.

### علت ۳ — دستور `profile` به موتورها وصل نبود

`SpeedProfileManager` ضریب‌ها را در حافظه‌ی خودش نگه می‌داشت، ولی **هیچ‌وقت**
چیزی مثل `axis->setSpeed(...)` صدا زده نمی‌شد. یعنی `profile fast` فقط یک
پیام چاپ می‌کرد.

---

## ۲) مشکلات ثانویه‌ای که همراهشان درست شد

| مشکل | اثر | اصلاح |
|---|---|---|
| `MIN_SPEED = 200` + منحنی S که تا نزدیکی صفر افت می‌کرد | اول و آخر **هر** حرکت بازو می‌خزید | پروفایل ذوزنقه‌ای واقعی `v² = v0² + 2as` با `RAMP_MIN_SPEED = 350` |
| منحنی کاهش سرعت نامتقارن (`1-0.7t²` بعد `0.3(1-t)²`) | نیم ثانیه خزیدن در انتهای هر حرکت | رمپ کاهش سرعت = قرینه‌ی دقیق رمپ شتاب |
| `AXIS_X_ACCELERATION = 700` | رمپ شتاب **۶۴ درجه** طول می‌کشید؛ یعنی هیچ حرکتی به سرعت بیشینه نمی‌رسید | شتاب‌های منطقی (۳۰۰۰ تا ۶۰۰۰) → رمپ حدود ۰.۲ تا ۰.۳ ثانیه |
| backoff هومینگ با `delayMicroseconds` **داخل ISR** | تا ۵.۲ ثانیه کل دستگاه (سریال، حلقه‌ی اصلی، همه‌ی محورها) قفل می‌شد | ماشین حالت کاملاً غیرمسدودکننده |
| `backoffFromEndstop()` حلقه‌ی `while` مسدودکننده | قفل شدن حلقه‌ی اصلی | نسخه‌ی غیرمسدودکننده |
| `sqrtf` + تقسیم `float` + `micros()` **داخل interrupt** | هر استپ حدود ۳۰µs هزینه داشت | جدول از پیش‌محاسبه‌شده؛ داخل ISR فقط جمع/تفریق صحیح |
| `digitalWrite`/`digitalRead` داخل ISR | کندی تولید پالس | رجیستر پورت مستقیم (`PORT`/`PIN`) که در `init()` محاسبه می‌شود |
| `_counter = _interval` (off-by-one) | دوره‌ی واقعی استپ `interval+1` تیک بود → سرعت همیشه ~۹٪ کمتر از مقدار تنظیم‌شده | `_counter = _interval - 1` |
| `AXIS_Y_SOFT_MAX = 8889` | معادل ۱۶۶ درجه! در حالی که محور Y فقط ۱۰۰ درجه است → **خطر برخورد** | `5333` (= ۱۰۰° × ۵۳.۳۳) |
| هومینگ هیچ حد ایمنی نداشت | اگر endstop خراب بود، موتور تا ابد حرکت می‌کرد | محدودیت مسافت جستجو + گزارش `HOMING FAILED` |
| تبدیل درجه↔استپ در `ROS_Interface.cpp` بدون `GEAR_RATIO` | دستورات ROS بین ۳ تا ۸ برابر بزرگ‌تر اجرا می‌شدند | استفاده از `getStepsPerDegree()` |
| دستور `timer` هیچ حرکتی انجام نمی‌داد | فقط پیام چاپ می‌کرد | وصل شدن به `motorController->moveTo` با callback |
| جدول `DEG_TO_STEPS` دستی در `.ino` | می‌توانست با `Config.h` ناهم‌خوان شود | محاسبه از خود محورها در `setup()` |

---

## ۳) نتیجه‌ی اندازه‌گیری‌شده

مدل تحلیلی روی کامپیتر (`python3 tools/sim_motion.py`) که منطق کد قدیم و
جدید را با زمان پیوسته مقایسه می‌کند:

> اعداد این بخش از مدل ریاضی‌اند. اندازه‌گیری‌های **دقیق** که با اجرای کد
> واقعی فریم‌ور به دست آمده‌اند در بخش ۶ آمده‌اند (اختلافشان در حد چند
> درصد است، چون مدل، گسستگی تیک ۲۰ کیلوهرتز را در نظر نمی‌گیرد).

### هومینگ (از دورترین نقطه‌ی مجاز تا صفر)

| محور | مسافت | قبل | بعد | بهبود |
|---|---|---|---|---|
| X | ۱۰٬۰۸۸ استپ | ۱۰۶ ثانیه | ۱۷ ثانیه | **۶.۲×** |
| Y | ۵٬۶۳۳ استپ | ۵۷ ثانیه | ۶.۶ ثانیه | **۸.۶×** |
| Z | ۴٬۱۱۱ استپ | ۴۱ ثانیه | ۷.۲ ثانیه | **۵.۷×** |
| A | ۵٬۴۰۰ استپ | ۵۷ ثانیه | ۱۲ ثانیه | **۴.۷×** |
| B | ۲٬۲۰۰ استپ | ۲۲ ثانیه | ۳.۵ ثانیه | **۶.۴×** |
| **مجموع** | | **۲۸۳ ثانیه** | **۴۶ ثانیه** | **۶.۱×** |

> بیشترین سهم زمان هومینگ مربوط به `AXIS_X_BACKOFF = 5200` است
> (۱۱۷ درجه عقب‌نشینی!). اگر این عدد واقعاً لازم نیست، کم کردنش
> هومینگ محور X را چند برابر سریع‌تر می‌کند.

و در تمام این مدت دیگر دستگاه **قفل نمی‌شود**؛ سریال زنده می‌ماند و
می‌توانی با `abort` هومینگ را لغو کنی.

### حرکت ۴۵ درجه

| محور | قبل | بعد |
|---|---|---|
| X | ۲.۷۳ ثانیه (۲۲.۵°/s) | **۱.۲۲ ثانیه (۴۶°/s)** |
| Y | ۳.۲۷ ثانیه | **۱.۴۷ ثانیه** |
| Z | ۵.۹۱ ثانیه | **۳.۳۴ ثانیه** |
| A | ۲.۸۲ ثانیه | **۱.۳۱ ثانیه** |
| B | ۲.۳۵ ثانیه | **۱.۱۱ ثانیه** |

حرکت هم‌زمان هر ۵ محور به اندازه‌ی ۴۵ درجه: **۳.۳۳ ثانیه**.

### حالا `MAX_SPEED` واقعاً اثر دارد

| `AXIS_X_MAX_SPEED` | زمان ۴۵ درجه — کد قدیم | کد جدید |
|---|---|---|
| 1000 | ۲.۳۴s | ۲.۰۵s |
| 2000 | ۲.۳۶s | ۱.۱۷s |
| 4000 | ۲.۴۰s | ۰.۹۲s |
| 8000 | ۲.۲۷s | ۰.۹۲s |

> در این جدول شتاب، همان `AXIS_X_ACCELERATION` ثابت است. در تست بخش ۶ برای
> هر سقف، شتاب دو برابر همان سقف گذاشته شده تا پروفایل مثلثی نشود؛ به همین
> دلیل زمان‌های آن جدول کمی متفاوت است.

---

## ۴) چطور سرعت را تنظیم کنی

### الف) از سریال، بدون فلش کردن مجدد (برای پیدا کردن حد دستگاه)

```
speeds                      → جدول سرعت مؤثر همه‌ی محورها
speed 150                   → ۱۵۰٪ سرعت کل دستگاه
profile slow|normal|fast    → ۵۰٪ / ۱۰۰٪ / ۱۵۰٪
maxspeed 1 3000             → MAX_SPEED محور ۱ = 3000 steps/s
accel 1 8000                → شتاب محور ۱ = 8000 steps/s²
homespeed 1 1200            → سرعت هومینگ محور ۱
status                      → سرعت لحظه‌ای هر محور (V=...) هم نشان داده می‌شود
```

این مقادیر **روی حرکت بعدی** اعمال می‌شوند (وسط حرکت ایمن نیست).

روش پیشنهادی برای پیدا کردن سقف واقعی دستگاه:
1. `speed 100` و یک حرکت کوتاه مثل `deg 1 20`
2. `speed 130` → تکرار. اگر موتور صدا داد یا استپ جا انداخت، برگرد عقب.
3. وقتی حد را پیدا کردی، همان را در `Config.h` بنویس.

> اگر موتور استپ جا بیندازد، موقعیت ثبت‌شده با موقعیت واقعی فرق می‌کند.
> بعد از هر تست خطرناک، یک `home` بزن.

### ب) از `Config.h`

```c
#define STEP_TICK_FREQ    20000L  // سقف سرعت = همین عدد (استپ بر ثانیه)
#define RAMP_MIN_SPEED    350     // سرعت شروع/پایان هر حرکت
#define STEP_PULSE_US     3       // پهنای پالس؛ برای TB6600 می‌تواند ۵ باشد

#define AXIS_X_MAX_SPEED      2000   // steps/s  (تقسیم بر 44.44 = درجه بر ثانیه)
#define AXIS_X_ACCELERATION   6000   // steps/s²
#define AXIS_X_HOMING_SPEED   900    // steps/s
```

فرمول‌های سرانگشتی:

```
درجه بر ثانیه  =  MAX_SPEED / (200 × MICROSTEP × GEAR_RATIO / 360)
طول رمپ (استپ) =  MAX_SPEED² / (2 × ACCELERATION)
زمان رمپ       =  MAX_SPEED / ACCELERATION
```

قاعده‌ی عملی: `ACCELERATION` را طوری انتخاب کن که زمان رمپ حدود
**۰.۲ تا ۰.۴ ثانیه** شود. با شتاب ۷۰۰ قبلی، رمپ ۲.۸ ثانیه و ۶۴ درجه بود!

---

## ۵) معماری جدید موتور حرکت

```
Timer1 (CTC, 20kHz)  ──►  MotorController::update()
                              │
                              ├─ بررسی estop هر 1ms (فقط هر ۲۰ تیک)
                              ├─ اگر هیچ محوری فعال نیست → خروج فوری
                              ├─ برای هر محور: Axis::tick()
                              │      └─ شمارنده‌ی تیک؛ وقتی صفر شد:
                              │            پین STEP بالا + به‌روزرسانی پروفایل
                              ├─ delayMicroseconds(STEP_PULSE_US)  ← یک‌بار برای همه
                              └─ پایین آوردن پین‌ها
```

* داخل ISR **هیچ** `micros()`، `sqrtf()`، تقسیم `float`، `Serial` یا `delay()`
  سنگینی وجود ندارد.
* جدول رمپ یک‌بار موقع صدور دستور حرکت (در حلقه‌ی اصلی) ساخته می‌شود.
* پهنای پالس STEP برای همه‌ی محورها **هم‌زمان** و با یک `delay` تولید می‌شود،
  نه پنج بار پشت سر هم.
* هومینگ یک ماشین حالت است:
  `RELEASE` (اگر endstop فشرده بود) → `SEARCH` → `BACKOFF` → `DONE`
  و در هر لحظه با `abort` قابل لغو است.

---

## ۶) تست‌های انجام‌شده

کد **واقعی** فریم‌ور (نه بازنویسی‌شده) روی host با g++ کامپایل، **لینک** و اجرا
می‌شود — این‌طوری هم خطای کامپایل و هم خطای لینک (`undefined reference`) قبل
از فلش گرفتن مشخص می‌شود. اجرا با:

```bash
bash tools/hosttest/run_tests.sh
```

| بخش | نتیجه |
|---|---|
| کامپایل هر ۱۴ ماژول با `-Wall -Wextra` | بدون خطا |
| **لینک کامل** (شامل `ROS_Interface.cpp` و خود اسکچ) | بدون `undefined reference` |
| اسموک‌تست `setup()` + `loop()` با **۹۰ دستور سریال** | بدون کرش |
| شبیه‌سازی رفتاری | **۵۹ بررسی، همه PASS** |

### اندازه‌گیری‌های شبیه‌سازی (تیک مجازی ۲۰ کیلوهرتز)

| تست | نتیجه |
|---|---|
| ترتیب اولویت هومینگ | دقیقاً `J1 -> J2 -> J3 -> J4 -> J5`؛ شروع محورها هم به همان ترتیب (هم‌زمان نه) |
| بک‌آف اجباری | بک‌آف هر ۵ محور ≥ `AXIS_x_BACKOFF`؛ `backoffDone=1`؛ endstop همه آزاد؛ `pos=0` |
| سوئیچ چسبیده حین بک‌آف | `HOMING FAILED: endstop stuck` — بی‌صدا صفر نشد، توالی متوقف شد |
| endstop خراب (پیدا نشد) | `HOMING FAILED: endstop not found` بعد از حد ایمنی |
| `home` روی محورهای قبلاً هوم‌شده | هر ۵ جوینت از نو و به همان ترتیب هوم شدند |
| تغییر ترتیب در زمان اجرا (`homeorder 3 2 1 4 5`) | ترتیب اجرا مطابق آن شد؛ ورودی تکراری/نامعتبر رد شد |
| هوم از روی endstop فشرده | آزادسازی خودکار، بعد بک‌آف و `pos=0` |
| هومینگ وسط حرکت | حرکت قبلی تمیز متوقف شد و هومینگ کامل انجام شد |
| حرکت ذوزنقه‌ای ۴۸۸۸ استپ | ۲.۶۷۸s عملی در برابر ۲.۶۷۱s تئوری (**۰.۳٪ خطا**)، اوج دقیقاً ۲۰۰۰ |
| سقف سرعت (۱۹۹۹ استپ) | ۱۰۰۰→۲.۲۱s، ۲۰۰۰→۱.۳۵s، ۴۰۰۰→۰.۹۹s، ۸۰۰۰→۰.۷۵s — هیچ‌کدام سریع‌تر از سقف |
| پروفایل‌ها | slow→۲.۱۴s، normal→۱.۲۳s، fast→۰.۹۶s |
| صحت پالس | هر پالسی که `tick()` اعلام کرد، واقعاً روی پین STEP رفت (شمارش مستقل) |
| تغییر هدف وسط حرکت | سرعت حفظ شد (۲۰۰۰→۲۰۰۰) و دقیقاً به هدف جدید رسید |
| استپ اضطراری | توقف فوری و قطعی در همان موقعیت |
| `moveTo` خارج از محدوده | رد شد |

---

## ۷) اولویت هومینگ و بک‌آف اجباری

### ترتیب: جوینت ۱ → ۲ → ۳ → ۴ → ۵

قبلاً ترتیب هومینگ `{2, 1, 0, 3, 4}` بود، یعنی **اول Z**، بعد Y، بعد X.
حالا ترتیب دقیقاً همان چیزی است که خواستی — به‌ترتیب شماره‌ی جوینت:

```c
// Config.h
#define HOMING_ORDER         {0, 1, 2, 3, 4}   // J1 -> J2 -> J3 -> J4 -> J5
```

هومینگ **کاملاً ترتیبی** است: جوینت بعدی فقط وقتی شروع می‌کند که جوینت قبلی
جست‌وجو **و بک‌آف** را تمام کرده باشد. هیچ‌وقت دو موتور هم‌زمان هوم نمی‌شوند.

از سریال هم قابل تغییر است (بدون فلش کردن مجدد):

```
homeorder                 → ترتیب فعلی را نشان می‌دهد
homeorder 1 2 3 4 5       → اول جوینت ۱ (پیش‌فرض)
homeorder 3 2 1 4 5       → اول Z (اگر مثلاً اول خواستی Z بالا برود)
```

### بک‌آف برای هر محور اجباری است

چرخه‌ی هر جوینت:

```
[endstop از قبل فشرده؟] --بله--> RELEASE: با سرعت کم از روی سوئیچ بلند شو
   SEARCH  : حرکت به سمت endstop با AXIS_x_HOMING_SPEED
   BACKOFF : مکث HOMING_DWELL_MS، سپس AXIS_x_BACKOFF استپ دور شدن از سوئیچ
   VERIFY  : endstop باید آزاد شده باشد → موقعیت := 0 → «هوم‌شده»
```

سه تضمین اضافه شد:

1. **هیچ محوری بدون بک‌آف صفر نمی‌شود.** تست، تعداد استپ‌های فاز بک‌آف هر
   محور را مستقل از کد فریم‌ور می‌شمارد و بررسی می‌کند که از
   `AXIS_x_BACKOFF` کمتر نباشد.
2. **آزاد شدن endstop تأیید می‌شود.** اگر بعد از بک‌آف سوئیچ هنوز فشرده بود،
   تا `HOMING_BACKOFF_EXTRA_STEPS` (=۴۰۰) استپ اضافه‌تر عقب می‌رود؛ و اگر باز
   هم آزاد نشد، هومینگ **با خطا** تمام می‌شود. قبلاً در این حالت بی‌سروصدا
   موقعیت «صفر» ثبت می‌شد در حالی که سوئیچ زیر فشار بود — یعنی کل مرجع موقعیت
   غلط بود و بعدش همه‌ی حرکت‌ها و soft limitها روی مختصات اشتباه کار می‌کردند.
3. **حلقه‌ی بی‌پایان حذف شد.** قبلاً اگر سوئیچ حین بک‌آف دوباره فعال می‌شد،
   کد `beginSearch()` صدا می‌زد و دستگاه می‌توانست تا ابد بین جست‌وجو و بک‌آف
   رفت‌وبرگشت کند. حالا آن حالت به‌عنوان «سوئیچ چسبیده» تشخیص داده و گزارش
   می‌شود.

### `home` حالا همه‌ی جوینت‌ها را از نو هوم می‌کند

قبلاً محورهای «هوم‌شده» رد می‌شدند. حالا `home` همیشه هر ۵ جوینت را
به‌ترتیب اولویت از نو مرجع می‌گیرد (و اگر لازم داری فقط یک جوینت، بزن
`home 3`). همراهش یک نکته‌ی وضعیت هم درست شد: در میانه‌ی توالی، پرچم
«هوم‌شده» همه‌ی محورها از همان لحظه‌ی شروع پاک می‌شود، پس `status` راستش را
می‌گوید که کدام جوینت هنوز مرجع ندارد.

اگر یک جوینت خطا بدهد، **کل توالی متوقف می‌شود** و بقیه هوم‌شده علامت
نمی‌خورند (چون مرجع دستگاه ناقص است):

```
!! Joint 1 HOMING FAILED: endstop stuck - never released, even during backoff
!! Homing sequence ABORTED - remaining joints were NOT homed
!! Fix the endstop, then send 'home' again
```

دلیل‌های خطایی که گزارش می‌شوند:

| پیام | معنی |
|---|---|
| `endstop not found within search limit` | سوئیچ هیچ‌وقت فعال نشد — سیم‌کشی/نصب/مسافت |
| `endstop stuck - never released, even during backoff` | سوئیچ در حال دور شدن هم فشرده ماند |
| `endstop still pressed after backoff` | بک‌آف برای هیسترزیس این سوئیچ کوتاه است |
| `emergency stop active` | `estop` فعال است — اول `reset` بزن |

> نکته درباره‌ی `AXIS_X_BACKOFF = 5200`: چون «صفر» در **انتهای بک‌آف** تعریف
> می‌شود، این عدد بخشی از هندسه‌ی دستگاه توست (۵۲۰۰ استپ ≈ ۱۱۷ درجه).
> تغییرش بده فقط اگر مطمئنی سوئیچ آن‌قدر از صفر کاری فاصله دارد.

---

## ۸) یک اصلاح دقتی دیگر: گسستگی سرعت در سرعت‌های بالا

تیک تایمر ۲۰ کیلوهرتز است، پس فاصله‌ی بین دو استپ باید **عدد صحیحی** از تیک
باشد و سرعت‌های ممکن گسسته‌اند (`20000/t`). قبلاً این تقسیم **به پایین**
گرد می‌شد، یعنی سرعت واقعی همیشه کمی **بیشتر** از مقدار درخواستی بود. در
سرعت‌های بالا این خطا بزرگ می‌شد:

| `MAX_SPEED` درخواستی | قبل (گرد به پایین) | حالا |
|---|---|---|
| ۲۰۰۰ | ۲۰۴۵ (+۲٪) | ۲۰۰۰ (دقیق) |
| ۴۰۰۰ | ۴۰۴۰ (+۱٪) | ۴۰۰۰ (دقیق) |
| ۸۰۰۰ | ۶۶۶۶ تا ۱۰۰۰۰ (+۲۵٪) | ۵۰۰۰ (هرگز بیشتر از ۸۰۰۰) |

قاعده‌ی جدید: در محدوده‌ی کاری معمول (`interval >= 8`، یعنی تا حدود
۲۵۰۰ استپ بر ثانیه) به **نزدیک‌ترین** عدد گرد می‌شود که دقیق‌ترین
زمان‌بندی را می‌دهد؛ در سرعت‌های بالاتر همیشه **به بالا** گرد می‌شود تا
سرعت واقعی هرگز از مقدار تنظیم‌شده بیشتر نشود. روی دستگاه واقعی، سریع‌تر
رفتن از سرعت تنظیم‌شده یعنی خطر جا ماندن استپ و از دست رفتن موقعیت.

دقت حرکت در محدوده‌ی کاری خودت دست‌نخورده ماند: حرکت ذوزنقه‌ای ۴۸۸۸ استپ
حالا ۲.۶۷۸ ثانیه در برابر ۲.۶۷۱ ثانیه‌ی تئوری (**۰.۳٪ خطا**).
